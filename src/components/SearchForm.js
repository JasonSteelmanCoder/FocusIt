import React from 'react';

const SearchForm = () => {
    const handleSubmit = () => {};

    return (
        <form onSubmit={handleSubmit}>
            <input></input>
            <button>Search</button>
        </form>
    )
};

export default SearchForm;